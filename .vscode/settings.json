// Workspace settings
{
    // The following will hide the js and map files in the editor
    "files.exclude": {
        ".nyc_output": true,
        "node_modules": true,
        "**/*.js.map": true
    }
}